[package]
name = "constantinn"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]

[profile.dev]
panic = "abort"
opt-level = 3
lto = true

[profile.release]
panic = "abort"
opt-level = 3
lto = true